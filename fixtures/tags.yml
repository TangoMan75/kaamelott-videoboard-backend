# https://fakerphp.github.io/formatters
App\Entity\Tag:
    tag_{1..100}:
        # https://schema.org/Thing
        name: <word()>_<current()>

        # Non standard ->
        clips: ['@clip_<current()>']
